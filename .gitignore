node_modules/
data/
dev/
.akidevsync/
.DS_Store

# standalone-packaging build output — regenerated per release, never committed
dist/

# local env overrides — .env.example is the only tracked template
.env
.env.*
!.env.example

# macOS custom folder icon resource
Icon?

# disposable visual export, regenerated wholesale
REPORT.html
